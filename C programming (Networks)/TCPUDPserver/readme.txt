Compile server with "gcc TCP_echo_server.c -o server"
Compile client with "gcc TCP_echo_client.c -o client"

To run server, type "./server "portno"
Run client after serer "./client "IP" "portno" "message"

IP= 127.0.0.1

Possibly wont work if you try using the same port no consecutively 